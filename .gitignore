# Generated by XcodeGen from project.yml - do not commit, regenerate with
# `xcodegen generate` (or automatically in CI, see .github/workflows/).
pantherapp.xcodeproj/

# Fetched from the build-ios-xray.yml artifact, or built locally - not
# source, and too large to commit (see project.yml's PantherTunnel target).
Frameworks/

# Fetched fresh in CI (build-ipa-full job) - not source, updates upstream
# periodically, too large to commit.
PantherTunnel/*.dat

# Standard Xcode/Swift build noise.
.build/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store
